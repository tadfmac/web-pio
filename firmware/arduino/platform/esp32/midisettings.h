#ifndef MIDI_SETTINGS_H
#define MIDI_SETTINGS_H

#include <MIDI.h>
#include <BLEMIDI_Transport.h>

// bleMidi::MySettings is already defined in BLEMIDI_Transport.h (Use1ByteParsing=false).
// No redefinition here — just pull in the headers so pio.h compiles cleanly.

#endif // MIDI_SETTINGS_H
